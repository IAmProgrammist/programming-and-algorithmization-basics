#include "libs/tester.h"

int main() {
    //SortFunc c = getRadixSFunc();
    //int a[] = {10, 4, 7, 2, 5, 6, 9, 1, 3, 8};
    //c.sort(a, 10);
    srand(clock());
    timeExperiment();
    return 0;
}